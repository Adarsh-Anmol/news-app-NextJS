export default function ModalDefaultPage(){
    return (
        null
        // this is required as if it doesn't return anything on external 
        //url or reload, then it will show an error and site won't load
    )
}